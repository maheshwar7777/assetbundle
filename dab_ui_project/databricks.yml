# This is a Databricks asset bundle definition for dab_ui_project.
# See https://docs.databricks.com/dev-tools/bundles/index.html for documentation.
bundle:
  name: dab_ui_project
  uuid: 14d0754f-7867-4b69-91d0-757d2a193505

include:
  - resources/*.yml
  - resources/*/*.yml

targets:
  dev:
    # The default target uses 'mode: development' to create a development copy.
    # - Deployed resources get prefixed with '[dev my_user_name]'
    # - Any job schedules and triggers are paused by default.
    # See also https://docs.databricks.com/dev-tools/bundles/deployment-modes.html.
    mode: development
    default: true
    presets:
      name_prefix: dev_${workspace.current_user.short_name}_
      source_linked_deployment: false
    workspace:
      host: https://dbc-116ec18e-9531.cloud.databricks.com

  prod:
    mode: production
    workspace:
      host: https://dbc-116ec18e-9531.cloud.databricks.com
      # We explicitly deploy to /Workspace/Users/kasireddymahesh7777@gmail.com to make sure we only have a single copy.
      root_path: /Workspace/Users/kasireddymahesh7777@gmail.com/PROD/.bundle/${bundle.name}/${bundle.target}
    permissions:
      - group_name: admins
        level: CAN_MANAGE
